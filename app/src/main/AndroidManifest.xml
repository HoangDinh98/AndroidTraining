<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flashdev">

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity -->
        <!-- android:name=".LoginSuccessActivity" -->
        <!-- android:label="@string/title_activity_login_success" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->

        <activity android:name=".Calculator" />
        <activity android:name=".FirstActivity" />
        <activity
            android:name=".Login"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calculate1" />
        <activity android:name=".Calculate2" />
        <activity android:name=".Home" />
        <activity android:name=".ListViewDemo" />
        <activity android:name=".RecyclerViewDemo" />
        <activity android:name=".SQLiteSimple" />
        <activity android:name=".ShowSQLData"></activity>
    </application>

</manifest>